A list of PKGBUILD files for Arch Linux

Build the by running ```makepkg``` in the respective directories.

Prepare a new package to upload to [AUR]http://aur.archlinux.org by running ```makepkg --source```
